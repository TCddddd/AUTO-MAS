# OK Script Adapter

AUTO-MAS plugin adapter for projects built on the `ok-script` framework.

The plugin parses project metadata at runtime, selects a supported CLI, source, or
legacy executable protocol, isolates user configuration, and preserves project-specific
providers only where they add verified behavior such as the OK-EF daily report handler.
