from mas.plugins import PluginConfigBase
from pydantic import ConfigDict, Field


class Config(PluginConfigBase):
    model_config = ConfigDict(extra="allow")

    hello: str = Field(default="world", description="问候词")
